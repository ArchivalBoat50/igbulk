"""igbulk — bulk downloader for public Instagram Reels and posts.

Three front ends over one engine:
  * `igbulk.cli`    — the `igdl` command
  * `igbulk.server` — local web UI with a paste box, plus a JSON API
  * `igbulk.mcp`    — MCP stdio server for AI agents
"""

__version__ = "1.0"
